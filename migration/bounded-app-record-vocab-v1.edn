{:schema "kotoba-atprotocol-migration/v1"
 :profile :atprotocol/bounded-app-record-vocab-v1
 :legacy {:source "src/atprotocol/app_record.cljc" :status :retained-general-oracle}
 :canonical {:kernel-and-conformance "src/atprotocol/bounded_app_record.kotoba"}
 :domain
 {:functions [:field-attr-table :field-count :known-field? :field->attr-or]
  :scope "the fixed 13-field camelCase-record-key -> :kotoba.app/* attribute
          projection vocabulary of the net.kotoba.app.manifest codec."
  :representation "[:map :string :keyword] canonical bounded typed-map
                   (string field name -> qualified-keyword attribute), the
                   same primitive already qualified by kotoba-lang/crdt /
                   states / sheets / edu-berkeley-spice, here with a keyword
                   VALUE."}
 :excluded [:atprotocol.app-record/record->manifest
            :atprotocol.app-record/manifest->record
            :atprotocol.app-record/attr->field
            :atprotocol.app-record/valid-record?
            :atprotocol.profile :atprotocol.projection]
 :exclusion-reason "the reduce-kv record<->manifest codec walks arbitrary open
                    input maps and preserves unknown fields under
                    :atprotocol/unprojected; valid-record? delegates to
                    kotoba.protocol.app/validate-manifest. Those, the reverse
                    attr->field direction, and the profile/projection layers
                    remain the CLJC oracle -- only the fixed forward
                    projection vocabulary is bounded here."
 :verification
 {:legacy {:tests 8 :assertions 52 :failures 0 :errors 0}
  :hand-verified-cases
  "main builds the fixed field-attr-table, asserts field-count 13, membership
   and correct attribute of \"bundleCid\" (:kotoba.app/bundle-cid) and \"sig\"
   (:kotoba.app/sig), and non-membership of the $type control key and an
   arbitrary unknown field (which falls back to :atprotocol/unknown)."
  :web {:main 42 :required-capabilities []}
  :wasm {:main 42}
  :required-capabilities []}
 :cutover {:bounded-profile-authorized true :general-cljc-oracle-retained true}}
